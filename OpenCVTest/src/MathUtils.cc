#include "MathUtils.h"

Quaterniond MathFunctionLibrary::GetDeltaQuad(Quaterniond PreQuad, Quaterniond CurrentQuad)
{
    Quaterniond result;
    
    return result;
}

Vector3d MathFunctionLibrary::GetDeltaTranslation(Vector3d PreTranslation, Vector3d CurrentTranslation)
{
    Vector3d result;

    return result;
}